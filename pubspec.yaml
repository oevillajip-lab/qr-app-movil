name: qr_app
description: Aplicacion QR + Logo
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0        
  image_picker: ^1.0.4 
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon.png